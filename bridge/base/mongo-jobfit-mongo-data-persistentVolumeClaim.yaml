#! mongo-jobfit-mongo-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mongo-jobfit-mongo-data
    namespace: jobfit-app
    labels:
        com.docker.compose.service: mongo
        com.docker.compose.volume: jobfit-mongo-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
